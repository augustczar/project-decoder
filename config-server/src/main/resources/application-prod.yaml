server:
  port: ${PORT}

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/augustczar/ead-config-server-repo
          default-label: develop
          username: ${GIT_USERNAME}
          password: ${GIT_PASSWORD}

encrypt:
  key: 8VQduMvRhvwljOwrKlLRqzQDsLQLPKezXyeUC8atrByPup75?f/UQq8w1xwOlIkjyCLGvI82GiSPgDMh9520iTzdga3-B!7yr78Nu6=sApO!jV5Ko1Q5mMrp1sJ7gxsN

ead:
  configServer:
    username: ${CONFIG_SERVER_USERNAME}
    password: ${CONFIG_SERVER_PASSWORD}
          
eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URL}
      
  instance:
    hostname: ${APP_DOMAIN_NAME}
    non-secure-port: 80    
    